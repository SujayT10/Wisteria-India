import { UpdatePartnerProfile } from './update-partner-profile';

describe('UpdatePartnerProfile', () => {
  it('should create an instance', () => {
    expect(new UpdatePartnerProfile()).toBeTruthy();
  });
});
